# Plan 50 — Teams + @team mentions (#79)

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development or executing-plans. Checkbox steps. TDD.

**Design (author's call):** reload.chat — Teams group members/agents for bulk access + `@team` mentions. Add `teams` + `team_members` (human|agent), team CRUD (admin-gated via RBAC #29), and **`@team` mention resolution**: mentioning a team fans a run out to the team's **agent** members (reuses the depth-guarded mention handler #27 + the fan-out idea #64). Org-scoped. Channel-access-by-team + team role assignment are follow-ups on this entity.

**Branch** `plan-50-teams` (off `main`). Postgres at `postgres://acp:acp@localhost:5432/acp`. Commit with `-c user.name="gagan114662" -c user.email="gagan@getfoolish.com"`.

---

## Task 0: teams data model + CRUD

**Files:** `services/app/src/db/schema.ts` + next migration (`0018_teams.sql` — confirm next contiguous), Create `src/teams/teams.ts`, `teams.test.ts`, `src/http/team-routes.ts`, `team-routes.test.ts`; Modify `src/server.ts`
- [ ] **Step 1 — schema/migration:** `teams` (id, orgId, name, createdAt) + `team_members` (orgId, teamId, memberKind text 'human'|'agent', memberId, PK (orgId,teamId,memberKind,memberId)). `pnpm db:migrate`.
- [ ] **Step 2 — `teams.ts`:** `createTeam(db,{orgId,name})`, `addTeamMember(db,{orgId,teamId,memberKind,memberId})` (verify the member/agent exists in org), `removeTeamMember(...)`, `listTeams(db,orgId)`, `teamAgentIds(db,orgId,teamName)` (the agent members of a team, by name — for @team mentions). All org-scoped.
- [ ] **Step 3 — routes:** `registerTeamRoutes(app,{db})`: `POST /teams {name}` (admin — `can(roleOf,"channel:create")` or add a `team:manage` action), `POST /teams/:id/members {memberKind,memberId}`, `DELETE /teams/:id/members/:kind/:mid`, `GET /teams`. Org-scoped (cross-org 404). Register in `server.ts`.
- [ ] **Step 4 — test:** create team + add an agent + a human → `listTeams`/`teamAgentIds` reflect it; cross-org team → 404; non-admin create → 403 (RBAC). `DATABASE_URL=… pnpm test` + tsc. Commit `feat(app): teams + team_members + CRUD routes (#79)`.

## Task 1: @team mention → fan out to the team's agents

**Files:** `services/app/src/chat/handle-mentions.ts`, `handle-mentions.test.ts`
- [ ] In `handleMentions` (#27), after resolving a mention handle: if it doesn't resolve to an agent, try `teamAgentIds(db, orgId, handle)` — if it's a team, start a run for EACH of the team's agent members (org-scoped, same repo/token resolution as a normal mention, depth+1, no self-trigger, dedup). The existing per-agent path is unchanged for non-team handles. Return all started run ids.
- [ ] **test:** a message `@backend-team` where the team has 2 agents on a repo → 2 runs started; a team with 0 agents → 0; depth guard + cross-org still hold; a normal `@agent` mention unchanged (existing tests green). `DATABASE_URL=… pnpm test` + tsc. Commit `feat(app): @team mentions fan out to team agents (#79)`.

---

## Self-Review
- Delivers #79's core: a Team entity + CRUD (admin-gated) and `@team` mentions that fan a run out to the team's agent members, reusing the depth-guarded mention handler (#27) + fan-out (#64). Org-scoped.
- Backward-compat: additive tables/module/routes; `handleMentions` only adds a team fallback when a handle isn't an agent (human mention path unchanged); org-scoped (#14). Migration additive. Existing suites green.
- Note: channel-access-by-team, team role assignment (RBAC scopes #29), and human-team notification fan-out are follow-ups on this entity.

## Definition of Done (79)
app suite green; tsc; migration applies. Teams CRUD (admin) + `@team` mention starts a run per team agent; org-scoped (cross-org 404, non-admin create 403); normal mentions unchanged.
